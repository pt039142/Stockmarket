services:
  - type: web
    name: prince-stock-exhange
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python serve_dashboard.py
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 10000
